class Hostel {
  final String name;
  final String description; // This will include facilities
  final bool isBoysHostel;
  // ... other fields you may want in the future (fees, etc.)

  Hostel({
    required this.name,
    required this.description,
    required this.isBoysHostel,
  });
}
